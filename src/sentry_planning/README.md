# sentry_planning 规划组

对应 legacy: `legacy/sentry_planning/src/sentry_planning/`。

迁移计划(按依赖顺序):
- trajectory_generation(全局规划)
- trajectory_tracking(局部规划/MPC)
- waypoint_generator(目标点生成)
- rviz_plugins(rviz2 插件)
